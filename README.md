# mlh-code-sample

- The code in this repository is piece of a HTTP web server I am currently building, it doesn't prove the HTTP layer yet, it's a TCP byte stream that easily adapt to any protocol, it has some http-related features like a minimal CGI interface,
 while other features are still in development.
